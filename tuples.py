# created a tuple using ()
h = (1, 2, 3, 4, 5)
# h1 = () empty tuple
h1 = (1)  # wrong way to declare a tuple with single element
h1 = (1,)  # Tuple with single element
print(h1)

# printing the elements of a tuple
print(h[3])

# cannot update the tuple values


# h[2] = 3 tuple object does not support item assignment
# print(h)
